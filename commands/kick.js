// const { MessageEmbed } = require('discord.js')
module.exports.aliases = ['kick', 'k']
// module.exports.run = (client, msg) => {
//   const args = msg.content.split(' ')
//   if (!msg.member.hasPermission('KICK_MEMBERS')) return
//   if (args[0] === 'help') {
//     msg.reply(`${config.prefix}kick <���� �Ǽ�>`)
//     return
//   }
//   const kUser = msg.guild.member(msg.mentions.users.first() || msg.guild.members.get(args[0]))
//   msg.guild.members.fetch(kUser).kick('����')
//   msg.channel.send(kUser + ' ������ ���������� ű �߽��ϴ�')
// }
